pandas
numpy
scipy
requests
beautifulsoup4
pyarrow
tqdm
python-dateutil
tabulate
streamlit
plotly
streamlit-autorefresh
certifi
pnsea
duckdb
feedparser
google-genai
websockets
fyers-apiv3>=3.0.0

# Dev/verification only — not imported by the nightly pipeline or the HUD,
# used by scripts/verify_greeks_vs_pyvollib.py to cross-check Greeks/IV
# against an independent reference implementation.
py_vollib

# Test suite — pytest (tests/test_oi_flow.py), playwright (scripts/verify_hud.py's
# headless-browser HUD parity check, run by nightly.sh after every EOD compile).
pytest
playwright
